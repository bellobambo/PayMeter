meta {
  name: List Features
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/features
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{token}}
}

tests {
  test("should list features successfully", function() {
    expect(res.status).to.equal(200);
    expect(res.body.success).to.be.true;
  });
}
