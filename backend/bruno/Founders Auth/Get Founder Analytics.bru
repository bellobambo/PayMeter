meta {
  name: Get Founder Analytics
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/api/founders/analytics
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{token}}
}

tests {
  test("should get analytics successfully", function() {
    expect(res.status).to.equal(200);
    expect(res.body.success).to.be.true;
  });
}
