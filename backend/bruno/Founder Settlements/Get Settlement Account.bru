meta {
  name: Get Settlement Account
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/api/founders/settlement/account
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{token}}
}

tests {
  test("should return settlement account or null", function() {
    expect(res.status).to.equal(200);
    expect(res.body.success).to.be.true;
  });
}
